apiVersion: opensearch.opster.io/v1
kind: OpenSearchCluster
metadata:
  name: {{ .Values.serviceName | default "opensearch" }}-cluster
  namespace: {{ .Values.namespace | default "default" }}
spec:
  general:
    version: {{ .Values.version }}
    httpPort: {{ .Values.httpPort }}
    vendor: opensearch
    setVMMaxMapCount: true
    serviceName: {{ .Values.serviceName | default "opensearch" }}
    pluginsList:
      - "repository-s3"
      - "https://github.com/aiven/prometheus-exporter-plugin-for-opensearch/releases/download/{{ .Values.version }}.0/prometheus-exporter-{{ .Values.version }}.0.zip"
    additionalConfig:
      discovery.seed_hosts: opensearch-cluster-masters-0
      {{/*need to be removed after deployment*/}}
      cluster.initial_master_nodes: opensearch-cluster-masters-0
  dashboards:
    version: {{ .Values.version }}
    enable: {{ .Values.dashboard.enabled }}
    replicas: {{ .Values.dashboard.replicas }}
    {{ if and .Values.username .Values.password }}
    opensearchCredentialsSecret:
      name: {{ .Values.serviceName | default "opensearch" }}-cluster-secret
    {{ end }}
    resources:
      requests:
        memory: "1Gi"
        cpu: "500m"
      limits:
        memory: "1Gi"
        cpu: "500m"
    additionalConfig:
      opensearch_security.multitenancy.enabled: "false"
  confMgmt:
    smartScaler: true
  {{ if and .Values.username .Values.password }}
  security:
    config:
      adminCredentialsSecret:
        name: {{ .Values.serviceName | default "opensearch" }}-cluster-secret
      {{ if .Values.passwordHash }}
      securityConfigSecret:
        name: {{ .Values.serviceName | default "opensearch" }}-securityconfig-secret
      {{ end }}
  {{ end }}
  nodePools:
    - component: masters
      replicas: {{ .Values.masters.replicas }}
      diskSize: "30Gi"
      nodeSelector:
      resources:
        requests:
          memory: "2Gi"
          cpu: "500m"
        limits:
          memory: "2Gi"
          cpu: "500m"
      roles:
        - "cluster_manager"
        - "data"
    - component: nodes
      replicas: {{ .Values.nodes.replicas }}
      diskSize: "30Gi"
      nodeSelector:
      resources:
        requests:
          memory: "2Gi"
          cpu: "500m"
        limits:
          memory: "2Gi"
          cpu: "500m"
      roles:
        - "data"
    - component: coordinators
      replicas: {{ .Values.coordinators.replicas }}
      diskSize: "30Gi"
      nodeSelector:
      resources:
        requests:
          memory: "2Gi"
          cpu: "500m"
        limits:
          memory: "2Gi"
          cpu: "500m"
      roles:
        - "ingest"
